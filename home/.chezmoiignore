.config/raycast

# Work-only / machine-local (never track in this public repo)
.config/fish/conf.d/work-local.fish

# Secrets — auth tokens, keys
.config/gh/hosts.yml
.config/op/**
**/*credentials*
**/*.pem
**/*.key

# OS and editor noise
.DS_Store
**/.DS_Store
**/*.swp
**/*.swo

# Skip work gitconfig on non-work machines
{{- if not .isWork }}
.gitconfig-work
{{- end }}
