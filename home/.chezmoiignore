.config/raycast

# Work-only / machine-local files (never track in this public repo)
.config/fish/conf.d/work-local.fish

# OS noise
.DS_Store
**/.DS_Store

# Skip work gitconfig on non-work machines
{{- if not .isWork }}
.gitconfig-work
{{- end }}
